// sum_arithmetic_sequences(first, last, num_of_terms) sums an
// arithmetic sequence
// requires: first ≤ last, difference be integer
int sum_arithmetic_sequences(int first, int last, int num_of_terms) {
    return (first + last) * num_of_terms / 2;
}

